# Environment variables for local development

# No secrets are required to run or deploy this app.
# The enquiry form sends email via Amazon SES using the Lambda's IAM role —
# there are no email passwords or API keys to configure.
